* intro note on first page
* auto-create project with post commit hook

* "working" spinner display for new project creation
* rdoc generation validation (what if it doesn't generate for some reason?)

* translate markdown / textile READMEs
* index pages for individual project owners
* refactor Project#update_rdoc

* indicate if post-commit hook is installed
* docs for different branches / tags?
